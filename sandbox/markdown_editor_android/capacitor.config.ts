import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.editor.markdown",
  appName: "Markdown Editor",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
